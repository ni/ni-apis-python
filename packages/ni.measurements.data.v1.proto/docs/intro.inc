Introduction
============

About
-----

The ``ni.measurements.data.v1.proto`` Python package contains generated Python interfaces for proto files in the ``ni-apis`` repo.

NI created and supports this package.

Operating System Support
------------------------

``ni.measurements.data.v1.proto`` supports Windows and Linux operating systems.

Python Version Support
----------------------

``ni.measurements.data.v1.proto`` supports CPython 3.10+.

Installation
------------

You can directly install the ``ni.measurements.data.v1.proto`` package using ``pip`` or by listing it as a dependency in
your project's ``pyproject.toml`` file.